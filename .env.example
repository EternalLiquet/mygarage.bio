# TODO: Set this to your Supabase project URL, e.g. https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_URL=
# TODO: Set this to your Supabase anon/public API key
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# TODO: Replace this local fallback with request-scoped auth client usage.
SUPABASE_USER_ACCESS_TOKEN=

# Required for signed public image URLs on private bucket + durable DB rate limits.
SUPABASE_SERVICE_ROLE_KEY=
# Optional: signed URL TTL in seconds (default 600, min 30).
PUBLIC_IMAGE_SIGNED_URL_TTL_SECONDS=600
